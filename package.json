{
  "name": "eve-connect-kernel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "imports": {
    "#*": "./agent/*"
  },
  "scripts": {
    "dev": "eve dev",
    "build": "eve build",
    "start": "eve start",
    "deploy": "eve deploy",
    "typecheck": "tsc",
    "connect:kernel": "vercel connect create kernel --name kernel-mcp --connection-method mcp && vercel connect attach kernel/kernel-mcp",
    "connect:slack": "export FF_CONNECT_ENABLED=1 && vercel connect create slack --name eve-connect-kernel --triggers && vercel connect detach slack/eve-connect-kernel --yes && vercel connect attach slack/eve-connect-kernel --triggers --trigger-path /eve/v1/slack --yes"
  },
  "dependencies": {
    "@onkernel/eve-extension": "^0.1.4",
    "@vercel/connect": "^0.4.0",
    "ai": "^7.0.26",
    "eve": "^0.27.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "^6.0.3"
  }
}
